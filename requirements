# See how to install with brew
# here intersting repo with many and many pentest tols and how to install it using brew 
# link: https://github.com/sidaf/homebrew-pentest

keepnote
nmap  (install vulners https://github.com/vulnersCom/nmap-vulners)
curl
smbclient
snmp-check
snmwpalker (look fr brutefiorce comunities )
masscan
sslyze
ssltest.py
joomscan 
wpscan
droopescan
gobuster

smbmap
enum4linux
snmp-check
snmwpalker (look fr brutefiorce comunities )
hydra
## add Upnp
impacket scripts (used from anywhere)
redis-exploit.py from https://github.com/roughiz/Redis-Server-Exploit-Enum (install it gloabally)
ssltest.py from https://github.com/Lekensteyn/pacemaker (install it globally)
wfuzz 
odat.py #####


# if script runs with sudo, run the keepnote as the sudo user
   if [ "$SUDO_USER" != "" ]; then
     chown -R ${SUDO_USER}:${SUDO_USER} $dir 
     if [ -f "${dir}notebook.nbk"  ]; then
       su -c "keepnote --no-show-errors --no-default  $dir &" $SUDO_USER && \
       exit 0
     fi  
   else
      if [ -f "${dir}notebook.nbk"  ]; then
        keepnote --no-show-errors --no-default $dir &
        exit 0
      fi  
   fi